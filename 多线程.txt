多线程的作用：一个任务在CPU上花时间，在IO上也会花时间，当IO时CPU闲置，所以必须要把这段时间利用起来，多线程可以保证资源利用率

线程私有：程序计数器、虚拟机栈、本地方法栈

线程状态：
创建、运行、阻塞、等待、超时等待、销毁

饿汉模式为了线程安全：
检验加锁检验 jvm重排序

构造方法默认线程安全

Synchronized底层实现是获取monitor（对象监视器）的所有权

SpringSingleton会有线程安全问题：
产生：多个线程操作同一个对象，对它的非静态成员变量的写操作存在线程安全问题
